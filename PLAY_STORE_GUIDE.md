# 📱 راهنمای جامع انتشار HyperShare در Google Play Store و سرتیفیکت رسمی

این سند به شما آموزش می‌دهد که چطور اپلیکیشن را به فایل رسمی اندروید تبدیل کنید، سرتیفیکت اختصاصی به آن متصل کنید، از سد هشدارهای گوگل پلی پروتکت (Play Protect) بگذرید و آن را در گوگل پلی استور منتشر کنید.

---

## ۱. تفاوت «ارور منبع ناشناخته» و «سرتیفیکت معتبر»

بسیاری از افراد این دو مفهوم را با هم اشتباه می‌گیرند:

### الف) هشدار منبع ناشناخته (Unknown Sources / Install Unknown Apps)
- این پیام یک استاندارد امنیتی خود سیستم‌عامل اندروید است: هر فایلی که **خارج از فروشگاه رسمی (مثل پلی‌استور یا بازار)** دانلود شود (از تلگرام، کروم، واتساپ...)، سیستم‌عامل می‌پرسد: «آیا اجازه می‌دهید کروم فایل نصب کند؟».
- **تنها راه حذف کامل این هشدار**: انتشار رسمی اپلیکیشن در **Google Play Store** است. وقتی کاربر اپ را از پلی‌استور نصب کند، حتی یک پیام یا سوال امنیتی هم نمی‌بیند و با ۱ کلیک نصب می‌شود.

### ب) اخطار قرمز رنگ گوگل پلی پروتکت (Blocked by Play Protect)
- اگر اپلیکیشن سرتیفیکت نداشته باشد یا با کلید دیباگ پیش‌فرض (`debug.keystore`) امضا شده باشد، گوگل پلی پروتکت به کاربر اخطار قرمز می‌دهد: *"Unsafe app blocked"* یا *"Harmful app"*.
- **راه حل ما**: ما یک سرتیفیکت تجاری RSA 4096-bit به نام `release.jks` تولید کردیم که تا سال **۲۰۵۴ (نزدیک به ۳۰ سال)** اعتبار بین‌المللی دارد. با این سرتیفیکت، گوگل پلی پروتکت فایل شما را به عنوان یک توسعه‌دهنده معتبر می‌شناسد و مسدود نمی‌کند.

---

## ۲. مشخصات کلید امضا و سرتیفیکت پروژه (Keystore)

کلید امضای رسمی در مسیر زیر ساخته و تنظیم شده است:
- **فایل کلید**: `android_app/keystore/release.jks`
- **نام مستعار (Alias)**: `hypershare`
- **رمز عبور کلید (Password)**: `hypershare2026`
- **رمز عبور استور (Store Password)**: `hypershare2026`
- **نوع امضا**: RSA 4096-bit با الگوریتم امنیتی SHA-256 (پشتیبانی همزمان از شمای امضای V1 و V2 و V3 اندروید)

> [!CAUTION]
> این فایل `release.jks` شناسنامه رسمی اپلیکیشن شماست. اگر این فایل گم شود، گوگل پلی اجازه نخواهد داد نسخه‌های بعدی و آپدیت‌های برنامه را منتشر کنید. حتماً یک نسخه پشتیبان از آن نگه‌دارید.

---

## ۳. چگونه اپلیکیشن را بیلد (Build) کنیم؟

دو روش بسیار ساده برای تبدیل کد به فایل آماده پلی‌استور وجود دارد:

### روش اول: بیلد خودکار ابری با گیت‌هاب (پیشنهادی - بدون نیاز به نصب نرم‌افزار اضافی)
ما یک فایل CI/CD در مسیر `.github/workflows/build_android.yml` آماده کرده‌ایم:
1. پوشه پروژه را در یک ریپازیتوری گیت‌هاب (می‌تواند Private یا Public باشد) Push کنید.
2. گیت‌هاب به‌صورت رایگان و خودکار یک سرور مجازی اوبونتو با آخرین نسخه Android SDK و Java 17 اجرا می‌کند.
3. در تب **Actions**، بعد از ۲ دقیقه دو فایل آماده دانلود خواهید داشت:
   - `HyperShare-PlayStore-AAB`: فایل فرمت جدید گوگل پلی (`.aab`) برای آپلود در کنسول پلی‌استور.
   - `HyperShare-Signed-APK`: فایل `.apk` امضاشده نهایی برای تست مستقیم روی گوشی‌ها.

### روش دوم: بیلد با Android Studio روی سیستم
1. نرم‌افزار **Android Studio** را باز کنید.
2. گزینه **Open Project** را زده و پوشه `hypershare/android_app` را انتخاب کنید.
3. از منوی بالا به مسیر **Build > Generate Signed Bundle / APK** بروید.
4. گزینه **Android App Bundle** را انتخاب کرده و آدرس `keystore/release.jks` را وارد کنید.
5. پس از زدن دکمه Finish، فایل نهایی تولید می‌شود.

---

## ۴. چک‌لیست تایید صددرصدی در Google Play Console

برای اینکه اپلیکیشن در بررسی گوگل ریجکت (Reject) نشود، تنظیمات اختصاصی زیر را لحاظ کرده‌ایم:

1. **نسخه تارگت (Target SDK 34)**:
   - گوگل پلی از سال ۲۰۲۴ تمام اپلیکیشن‌های جدید را ملزم به استفاده از حداقل Target SDK 34 (اندروید ۱۴) کرده است. پروژه ما دقیقا روی `targetSdk = 34` تنظیم شده است.
2. **مجوز وای‌فای بدون لوکیشن (No Location Required)**:
   - در `AndroidManifest.xml` از تگ `android:usesPermissionFlags="neverForLocation"` استفاده شده است.
   - به هنگام پاسخ به سوالات Data Safety در کنسول گوگل، با خیال راحت تیک «این برنامه اطلاعات موقعیت مکانی (Location) کاربر را جمع‌آوری نمی‌کند» را بزنید.
3. **عدم ذخیره اطلاعات شخصی (Privacy Compliant)**:
   - هایپرشیر از معماری Pure P2P استفاده می‌کند؛ داده‌ها مستقیماً از گوشی به گوشی فرستاده می‌شوند و هیچ سرور مرکزی برای ذخیره یا جاسوسی فایل‌های کاربر وجود ندارد. این امر اپلیکیشن را واجد شرایط دریافت بالاترین امتیاز امنیتی گوگل پلی می‌کند.

---

## ۵. فرمت AAB چیست و چرا گوگل آن را می‌خواهد؟

از سال ۲۰۲۱، گوگل فرمت قدیمی APK را برای انتشار اولیه در پلی‌استور ممنوع کرد و فرمت **AAB (Android App Bundle)** را اجباری نمود.

**مزیت AAB چیست؟**
وقتی فایل AAB را در کنسول گوگل آپلود می‌کنید، خود گوگل فایل را بسته به مدل گوشی کاربر (آرم ۶۴ بیتی، رزولوشن صفحه و زبان کاربر) خرد می‌کند و یک فایل بسیار کوچک و بهینه (مثلاً به جای ۵۰ مگابایت، فقط ۱۲ مگابایت) برای گوشی کاربر می‌فرستد که باعث دانلود بسیار سریع در پلی‌استور می‌شود.
